# Used by Docker Compose. Set a domain whose DNS points to your server.
DOMAIN=museum.example.com
# For running Node directly, set NODE_ENV=production and PUBLIC_ORIGIN=https://your-domain
# DATABASE_PATH must refer to persistent storage. Node does not auto-load this .env.
